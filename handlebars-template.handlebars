<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css" integrity="sha512-xh6O/CkQoPOWDdYTDqeRdPCVd1SpvCA9XXcUnZS2FmJNp1coAFzvtCN9BmamE+4aHK8yyUHUSCcJHgXloTyT2A==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        body {
            background-color: #f8f9fa; 
        }
        .container {
            margin-top: 50px;
        }
        #BrandLogo {
            max-width: 400px; 
            height: auto; 
            margin: 0 auto 30px; 
            display: block;
        }
        .section {
            margin-bottom: 30px;
            padding: 20px;
            background-color: #ffffff; 
            border-radius: 10px; 
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1); 
        }
        .btn-primary {
            background-color: #e22866;
            border-color: #e22866;
        }
        .btn-primary:hover {
            background-color: #d91e5a;
            border-color: #d91e5a;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-md-8 offset-md-2"> 

                {{#if profile}}
                    <img id="BrandLogo" src="your-logo.png" alt="Brand Logo"> 

                    <div class="section">
                        <h2 class="text-center">Welcome to the {{profile.current_sailing.ship}}, {{profile.given_name}}!</h2>
                    </div>

                    <div class="section">
                        <h3>Your Cabin</h3>
                        <p>You are staying in cabin {{profile.current_sailing.cabin}}</p>
                    </div>

                    {{#if profile.ageVerified}}
                        <div class="section">
                            <h3>Gaming</h3>
                            <p>Thank you for verifying your age. You can now access gaming services.</p>
                        </div>
                    {{else}}
                        <div class="section">
                            <h3>Age Verified Services</h3>
                            <p>Please see an onboard staff member to verify your age to access these services.</p>
                        </div>
                    {{/if}}

                    <div class="section">
                        <h3>Peninsular</h3>
                        <p>Your current Peninsular tier is <b>{{profile.template.tier}}</b>. You have a <b>{{profile.spendDiscount}}</b> discount on your onboard spend.</p>
                    </div>

                    <div class="section">
                        <h3>Profile Data</h3>
                        <ul>
                            {{#each profile}}
                                <li><b>{{@key}}</b>: {{this}}</li>
                            {{/each}}
                        </ul>
                    </div>

                    <div class="section text-center">
                        <a href="/logout" class="btn btn-primary btn-lg">Logout</a>
                    </div>

                {{else}}
                    <img id="BrandLogo" src="your-logo.png" alt="Brand Logo"> 

                    <div class="section">
                        <h2 class="text-center">Welcome!</h2>
                    </div>

                    <div class="section text-center">
                        <a href="/protected" class="btn btn-primary btn-lg">Login</a>
                    </div>
                {{/if}}

            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
</body>
</html>
